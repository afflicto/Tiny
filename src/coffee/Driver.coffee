class Tiny.Store.Driver

	# get a bunch of records
	get: (type, offset = 0, limit = 20, callback) ->
		
	# find a given record by ID
	find: (type, id, callback) ->
	
	# store a record
	store: (type, attributes, callback) ->
